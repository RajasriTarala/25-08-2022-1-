#include<stdio.h>
void main()
{
	int i,j,space=0;
	int count=0;
	char str[10];
	printf("Enter anything:\n");
	scanf("%d",&i);
	puts(str);
	for(i=0;i<10;i++)
	{
		space=j;
		
      	if(str[i]==space)             //if(str1[i]==' ') 
	    {
	    	count++;
	    }
        //if(count==0)
             printf("Output is:",i);
              puts(i);
          }
	}
